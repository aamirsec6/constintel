# Railway Environment Variables Template
# Set these variables in Railway Dashboard → Your Service → Variables
# Railway automatically provides: $DATABASE_URL and $REDIS_URL when you add PostgreSQL/Redis services

# ============================================
# RAILWAY AUTO-PROVIDED (DO NOT SET MANUALLY)
# ============================================
# DATABASE_URL - Automatically set when you add PostgreSQL service
# REDIS_URL - Automatically set when you add Redis service
# RAILWAY_PUBLIC_DOMAIN - Public domain for your service
# RAILWAY_ENVIRONMENT - Environment name (production, preview, etc.)

# ============================================
# REQUIRED - SET THESE IN RAILWAY DASHBOARD
# ============================================

# Environment
NODE_ENV=production
LOG_LEVEL=info

# Security (REQUIRED - Generate strong secrets!)
# Generate with: openssl rand -base64 32
JWT_SECRET=your-strong-jwt-secret-min-32-chars
ENCRYPTION_KEY=your-strong-encryption-key-min-32-chars

# Frontend Public URLs (SET AFTER DEPLOYMENT)
# Replace with your actual Railway service URLs
# Backend service URL (e.g., https://backend-production.up.railway.app)
NEXT_PUBLIC_API_URL=https://your-backend-service.up.railway.app
# ML service URL (e.g., https://ml-service-production.up.railway.app)
NEXT_PUBLIC_ML_API_URL=https://your-ml-service.up.railway.app

# ============================================
# OPTIONAL - EMAIL CONFIGURATION
# ============================================
EMAIL_PROVIDER=smtp
EMAIL_FROM=noreply@constintel.com
EMAIL_FROM_NAME=ConstIntel

# SMTP Configuration (if EMAIL_PROVIDER=smtp)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=true
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password

# SendGrid Configuration (if EMAIL_PROVIDER=sendgrid)
# SENDGRID_API_KEY=your-sendgrid-api-key

# AWS SES Configuration (if EMAIL_PROVIDER=ses)
# AWS_SES_HOST=email-smtp.us-east-1.amazonaws.com
# AWS_SES_USER=your-ses-user
# AWS_SES_PASS=your-ses-password

# ============================================
# OPTIONAL - INTEGRATION SECRETS
# ============================================

# Shopify Integration
SHOPIFY_API_KEY=
SHOPIFY_API_SECRET=
SHOPIFY_WEBHOOK_SECRET=
SHOPIFY_STORE_DOMAIN=

# WooCommerce Integration
WOOCOMMERCE_API_URL=
WOOCOMMERCE_CONSUMER_KEY=
WOOCOMMERCE_CONSUMER_SECRET=

# Twilio Integration
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
TWILIO_WHATSAPP_FROM=

# ============================================
# OPTIONAL - FEATURE FLAGS
# ============================================
ENABLE_AUTO_MERGE=true
MAX_AUTO_MERGE_PROFILES=3
ENABLE_ML_PREDICTIONS=true

# ============================================
# OPTIONAL - LLM/OLLAMA CONFIGURATION
# ============================================
# Note: Ollama typically runs locally, so these may not be needed on Railway
# OLLAMA_URL=http://localhost:11434
# OLLAMA_MODEL=llama3:8b
# OLLAMA_ENABLED=false
# ENABLE_LLM_INSIGHTS=false
# ENABLE_LLM_QUERY=false

# ============================================
# ML SERVICE CONFIGURATION
# ============================================
ML_SERVICE_PORT=8000
ML_MODEL_PATH=/app/models
ML_FEATURE_STORE_PATH=/app/features

# ============================================
# DEPLOYMENT NOTES
# ============================================
# 1. Railway automatically provides DATABASE_URL and REDIS_URL
# 2. Set NEXT_PUBLIC_API_URL and NEXT_PUBLIC_ML_API_URL after deploying services
# 3. Generate strong secrets for JWT_SECRET and ENCRYPTION_KEY
# 4. Set environment variables in Railway Dashboard → Service → Variables
# 5. You can also set variables via Railway CLI: railway variables set KEY=value

